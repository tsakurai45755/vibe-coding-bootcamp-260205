# 日報アプリMVP 実装タスク

## 0. 方針
- MVPを最短で動かす（1ページ＋エクスポート、画像1枚）
- DBなし
- 設定は環境変数

---

## 1. 仕様確定（Docs）
- [ ] `docs/spec/requirements.md` のTBDを埋める
  - 画像上限（MB/px/形式）
  - タイムアウト/リトライ方針
  - 一時保持の対象（Markdown保持の要否）
- [ ] `docs/spec/design.md` のTBDを埋める
  - Azure運用ルール（azure.instructions.md）反映
  - デプロイ方式（コード/コンテナ）

---

## 2. リポジトリ雛形
### 2.1 ディレクトリ
- [ ] `frontend/`（React）を作成
- [ ] `backend/`（FastAPI）を作成

### 2.2 共通（開発体験）
- [ ] ルートに簡易README（起動方法）を作成
- [ ] フロント: `npm run dev` で `http://localhost:3000`
- [ ] バック: `uvicorn` で `http://localhost:8000`

---

## 3. バックエンド（FastAPI）
### 3.1 依存関係
- [ ] FastAPI/uvicorn の導入
- [ ] 画像処理（必要なら Pillow）
- [ ] Azure OpenAI SDK（推奨）またはHTTPクライアント（設計に従う）

### 3.2 API
- [ ] `GET /healthz` 実装
- [ ] `POST /api/generate` 実装
  - [ ] multipart 受理
  - [ ] バリデーション（形式/サイズ/破損）
  - [ ] 前処理（最小）
  - [ ] Azure OpenAI 呼び出し
  - [ ] 出力整形（3見出し）
  - [ ] エラーレスポンス統一

### 3.3 設定
- [ ] 環境変数の読み込み（必須/任意の検証）
- [ ] CORS設定（開発用）
- [ ] タイムアウト設定

### 3.4 ログ/セキュリティ
- [ ] 画像データ/キーをログに出さない
- [ ] 例外時のログは最小限（スタックトレースは開発時のみ）

### 3.5 テスト
- [ ] 画像検証ユニットテスト
- [ ] 整形ユニットテスト

---

## 4. フロントエンド（React）
### 4.1 雛形
- [ ] React + TypeScript（Vite想定）をセットアップ
- [ ] 依存: 可能なら最小（UIは標準コンポーネント中心）

### 4.2 画面（1ページ）
- [ ] 画像アップロード
  - [ ] ドラッグ&ドロップ
  - [ ] 貼り付け
  - [ ] ファイル選択
  - [ ] プレビュー
- [ ] 生成ボタン / ローディング
- [ ] エラー表示
- [ ] 結果表示/編集（Markdownテキスト）
- [ ] コピー
- [ ] エクスポート（.mdダウンロード）

### 4.3 API連携
- [ ] `/api/generate` 呼び出し
- [ ] タイムアウト/エラー時の表示

### 4.4 テスト（可能なら）
- [ ] 主要状態遷移のテスト

---

## 5. 検証（MVP完了条件）
- [ ] 画像1枚→生成→編集→コピー→.md保存が通る
- [ ] 非画像/サイズ超過/タイムアウト/モデル失敗で適切にエラー表示される
- [ ] `/healthz` が200を返す

---

## 6. デプロイ準備（App Service）
- [ ] App Service向け起動コマンド・ポート設定を整理
- [ ] 環境変数一覧をドキュメント化
- [ ] Azure運用ルール（azure.instructions.md）に合わせて修正
